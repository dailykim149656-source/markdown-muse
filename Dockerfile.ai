FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

ENV NODE_ENV=production
ENV AI_SERVER_PORT=8080

EXPOSE 8080

CMD ["node", "--import", "tsx", "server/aiServer.ts"]
